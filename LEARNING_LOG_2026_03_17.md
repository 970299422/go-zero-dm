# 2026-03-17 学习总结：从“能跑”到“可维护”的微服务联调闭环

这期学习的核心，不是单点新功能，而是把之前零散可用的能力整合成了一个更接近生产实践的完整链路：

- API <-> RPC <-> DB 全链路稳定可跑
- 注册逻辑从 API 下沉到 RPC，并标准化为 gRPC
- 鉴权与用户信息查询链路打通
- Redis 缓存接入并做降级保护
- 前端登录页与后端接口完成真实联调

---

## 1. 本期完成了什么

### 1.1 全链路问题排查与稳定化

你完成了真实工程中最关键的一步：不是“写新代码”，而是先把链路跑顺。

- 修正接口路径认知偏差（`/api/user/*`）
- 解决 `no such table: user`（本质是多份 `identity.db` + 服务连接不一致）
- 统一 API/RPC 的 `DataSource` 指向同一数据库文件
- 在 RPC 侧补上 `AutoMigrate`，避免首次运行无表

> 收获：联调问题里，80% 不是业务逻辑本身，而是环境、配置、端口和依赖关系。

### 1.2 JWT 鉴权链路完善

你把“登录拿 token”推进到了“token 真正参与业务授权”。

- 新增 JWT 解析能力（ParseToken）
- 轻量中间件从 `Authorization` 提取并解析 token
- 将 `userId` 注入上下文，供 `userinfo` 等接口使用

> 收获：认证不仅是“发 token”，还包含“在请求生命周期里正确消费 token”。

### 1.3 注册逻辑微服务化（关键进阶）

这期最重要的架构升级：把注册从 API 直连 DB 改为 API 调 RPC。

阶段经历：

1. 临时过渡方案：API 调 RPC 内部 HTTP helper（快速验证职责下沉）
2. 标准化方案：改为 gRPC `CreateUser`（最终方案）

最终落地：

- `identity.proto` 增加 `CreateUser` 接口与消息
- 生成并接通 `pb` / grpc stub
- RPC 侧实现 `CreateUser`（查重 + bcrypt + 持久化）
- API `registerlogic` 改为调用 `IdentityRpc.CreateUser`
- 删除 RPC 临时 HTTP helper，避免双协议并存

> 收获：迁移时可以“先过渡后标准化”，但最终要回到统一协议，减少维护成本。

### 1.4 Redis 用户信息缓存接入

你完成了 userinfo 的 Cache-Aside 模式：

- 读路径：先查 Redis，命中直接返回
- 未命中：调 RPC 拿数据，再回填 Redis（TTL 10 分钟）
- key 设计：`user:info:{userId}`

同时做了非常关键的工程保护：

- Redis 客户端设置短超时（100ms）
- 禁止重试（`MaxRetries: 0`）
- 读写缓存使用独立超时上下文

> 收获：缓存不是“越快越好”，而是“缓存挂了也不能拖垮主链路”。

### 1.5 前端联调修正

你完成了从“接口可用”到“页面可用”的关键对齐：

- 修复前端 API 路径与后端不一致问题
- 修复登录响应字段映射（`accessToken`）
- 兼容后端直接返回业务对象（非统一 `{code,data,message}` 包装）
- 完成 `pnpm build` 验证

> 收获：前后端联调的本质是“协议一致性”（路径、字段、响应格式）。

---

## 2. 这期踩过的典型坑（非常有价值）

### 2.1 protobuf / proto 文件问题

你遇到了：

- `Expected top-level statement`
- `Multiple package definitions`

本质是 proto 内容重复/结构损坏，外加生成文件与声明不一致。

### 2.2 端口冲突与错误服务实例

你多次遇到 `bind: Only one usage...`，并且有“看起来服务启动了但协议不对”的情况（HTTP/1.1 和 gRPC 端口混淆）。

### 2.3 PowerShell 测试字段取值错误

`$login.data.accessToken` 与真实返回不一致，导致后续 `userinfo` 401。

### 2.4 Redis 未启动导致 userinfo 慢请求

初版缓存接入在 Redis 不可用时造成超时，后续通过 fail-fast 修复。

> 这些坑都很真实，能完整走一遍，说明你已经在做“工程化学习”，不是只学语法。

---

## 3. 本期能力升级（你已经具备）

1. 能独立定位 API/RPC/DB 三层联调问题
2. 能完成从单体逻辑到 RPC 逻辑的迁移
3. 能理解并实施 gRPC + Protobuf 的契约驱动开发
4. 能设计基础缓存方案并考虑降级与超时
5. 能完成前后端协议对齐与页面级验证

---

## 4. 当前阶段定位（对应学习计划）

根据 `LEARNING_GUIDE.md`：

- Phase 1-4：完成
- Phase 5：已完成核心目标（鉴权 + 缓存 + 全局联调）
- 当前进入：**Phase 5 收尾 / Phase 6 准备**

---

## 5. 下一步建议（复学友好版）

### 5.1 优先做的 3 件事

1. 缓存一致性补全：在“用户信息更新”后删除 `user:info:{id}`
2. 启动脚本统一：用一个脚本同时拉起 RPC/API/前端，减少端口与目录误操作
3. Docker Compose 化：把 Redis、API、RPC 纳入一套可复现环境

### 5.2 可选进阶

- 引入 Etcd 服务发现（替代直连 Endpoints）
- 统一错误码与响应包装（前后端协议进一步标准化）
- 给关键链路补最小化测试（注册、登录、userinfo、缓存命中）

---

## 6. 一句话总结

这期你完成了从“会写接口”到“能维护微服务链路”的跃迁。

重点不是多写了几个接口，而是你已经开始按生产系统思维做事：

- 先联通，再规范；
- 先可用，再优化；
- 遇到故障能定位并收敛到稳定方案。

继续保持这个节奏，下一阶段进入部署和运维会非常顺。