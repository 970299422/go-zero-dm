# 2026-03-19 学习总结：Redis 在 Demo API 里的真实作用

这次学习不再停留在“Redis 很快”这种抽象认识，而是通过 `GET /demo/item/:id` 和 `PUT /demo/item/:id` 两条链路，把 Redis 放进了一个真实、最小、可验证的 API 场景里。

核心收获是：Redis 在这个 Demo 中不是“真数据源”，SQLite 才是；Redis 是一个可失效、可重建、可降级的缓存层。

---

## 1. 这次真正做了什么

围绕 `backend/app/demo/api`，完成了一个最小闭环：

- `GET /demo/item/:id`
  - 先查 Redis
  - 没命中就查 SQLite
  - 再把结果写回 Redis
- `PUT /demo/item/:id`
  - 先更新 SQLite
  - 再删除对应 Redis key
- Redis 不可用时
  - 仍然可以直接查 SQLite
  - 主链路不被缓存拖垮

也就是说，这次不是“接了 Redis”，而是完整验证了：

- 命中
- 失效
- 降级

---

## 2. 我现在对 Redis 的理解

### 2.1 在 `GetItem` 中，Redis 的作用是加速读

`GetItem` 的流程现在是：

```text
HTTP 请求
-> handler
-> logic
-> 先查 Redis
-> 没命中再查 SQLite
-> 返回结果
-> 顺手写回 Redis
```

这说明 Redis 在这里的职责是：

- 减少重复查 SQLite
- 提高同一条数据的重复读取速度
- 作为性能优化层，而不是正确性来源

### 2.2 在 `UpdateItem` 中，Redis 的作用是清理旧副本

`UpdateItem` 的流程是：

```text
先更新 SQLite
-> 删除 demo:item:{id}
-> 下次 GET 时重新查 SQLite
-> 再重建缓存
```

这让我真正理解了：

- 更新接口不是“负责把 Redis 也改对”
- 更新接口更常见的职责，是把旧缓存删掉

---

## 3. 这次亲手验证的 3 个场景

### 3.1 命中 / 回填

第一次请求：

```text
GET /demo/item/1
```

因为 Redis 里还没有缓存，所以会：

- 查 SQLite
- 返回结果
- 把结果写进 Redis

之后再次请求同一个 `id`，理论上就可以直接命中缓存。

### 3.2 失效

执行：

```text
PUT /demo/item/1
```

更新 SQLite 后，删除 Redis key。  
这样下一次 `GET /demo/item/1` 时，就不会读到旧缓存，而是重新回源 SQLite。

这就是最基础的缓存一致性策略。

### 3.3 降级

把 Redis 停掉后，再请求：

```text
GET /demo/item/1
```

接口仍然能返回正确结果。

这说明：

- Redis 挂了
- 但主链路还能工作
- 缓存只是优化层，不是系统单点依赖

这是我这次觉得最有工程价值的地方。

---

## 4. 为什么“删除缓存”比“直接更新缓存”更常见

这次我第一次真正理解这件事。

### 4.1 删除缓存更简单

更新数据时，只需要：

- 更新 SQLite
- 删除对应缓存

下次读取时自动重建缓存。

逻辑非常直接。

### 4.2 删除缓存更不容易错

如果选择“更新 SQLite 后，再更新 Redis”，就会出现双写一致性问题：

- SQLite 成功，Redis 失败
- Redis 成功，SQLite 失败

都会导致数据不一致。

而删除缓存的策略更稳：

- 数据库永远是准的
- 缓存只是旧副本
- 旧副本删掉后，下一次读取自然恢复正确状态

### 4.3 删除缓存更适合复杂系统

真实系统里，一条数据可能对应很多缓存 key：

- 详情页缓存
- 列表页缓存
- 聚合页缓存
- 用户视角缓存

如果每次更新都要“同步更新所有缓存”，维护成本会非常高。  
而“失效”只需要先删掉关键缓存，让后续读请求逐步重建。

---

## 5. 这次踩到并理解的工程点

### 5.1 资源要显式关闭

在给 Demo API 加 SQLite 时，测试一开始失败，不是业务逻辑错，而是：

- SQLite 文件被占用
- 测试结束时删不掉临时数据库文件

最后通过给 `ServiceContext` 增加 `Close()` 解决。

这让我理解到：

- 谁创建资源，谁负责释放资源
- 后端工程里，数据库连接、文件句柄、网络连接都要关注生命周期

### 5.2 Redis 不能无限等待

这次配置了 Redis 的：

- `DialTimeout`
- `ReadTimeout`
- `WriteTimeout`
- `MaxRetries: 0`

我学到的是：

- 缓存失败可以接受
- 但缓存拖慢主链路不可以接受

所以“fail-fast”比“努力重试”更符合这个 Demo 的目标。

---

## 6. 我现在形成的完整心智模型

```text
读请求：
先查 Redis
-> 命中：直接返回
-> 未命中：查 SQLite
-> 把结果写回 Redis

写请求：
先更新 SQLite
-> 删除 Redis key

Redis 故障：
跳过缓存
-> 直接查 SQLite
```

一句话总结：

> Redis 在这个 Demo API 里，是一个加速读取、允许失效、允许故障、但不能影响正确性的缓存层。

---

## 7. 这次学习后我真正掌握的能力

1. 能解释 Redis 为什么放在 `GetItem` 里，而不是直接替代数据库。
2. 能解释为什么 `UpdateItem` 要删缓存，而不是直接改缓存。
3. 能解释什么叫缓存命中、缓存失效、缓存降级。
4. 能看懂一个最小 Cache-Aside 查询链路。
5. 能从“代码层面”而不是“概念层面”理解 Redis 的职责。

---

## 8. 下一步建议

如果继续往下学，最自然的两个方向是：

1. 给这套 Demo API 补一个最小的“缓存命中验证”方法，例如直接用 `redis-cli` 看 `demo:item:{id}`。
2. 把这套 Demo 再推进一步，补一个更完整的测试：验证 `GET -> PUT -> GET` 的缓存失效闭环。
