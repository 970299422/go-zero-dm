syntax = "v1"

info (
	title:   "demo"
	desc:    "demo api"
	author:  "codex"
	version: "v1"
)

type (
	ItemResp {
		id        int64
		name      string
		updatedAt string
	}
	GetItemReq {
		Id int64 `path:"id"`
	}
	UpdateItemReq {
		Id   int64  `path:"id"`
		Name string `json:"name"`
	}
)

@server (
	prefix: /demo
)
service demo-api {
	@handler GetItem
	get /item/:id (GetItemReq) returns (ItemResp)

	@handler UpdateItem
	put /item/:id (UpdateItemReq) returns (ItemResp)
}

