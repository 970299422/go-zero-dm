Name: identity-api
Host: 0.0.0.0
Port: 8888

# 数据库配置
DataSource: "identity.db"

# JWT Auth Configuration
JwtAuth:
  AccessSecret: "uKq9785j8X9d8s7s6f5g4h3j2k1l" 
  AccessExpire: 86400

# RPC 客户端配置 (直连模式，不走 Etcd)
IdentityRpc:
  Endpoints:
    - 127.0.0.1:8080
  NonBlock: true
