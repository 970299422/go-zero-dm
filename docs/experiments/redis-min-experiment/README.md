# Redis 最小实验

## 观察记录
- `EXPIRE user:info:1 5` 后，`TTL` 立即返回 `5`，说明过期时间已成功设置；随着时间流逝 TTL 会递减。
- 等待 `6s` 后再 `GET user:info:1`，键已过期，结果为 `nil`/空值，说明 Redis 会在过期时间到达后自动删除该键。

## 一句话总结
- 这个实验验证了 Redis 的 `EXPIRE` 过期机制会按设定时间生效，并在到期后自动清理键。
